image: node:20

pipelines:
  default:
    - step:
        name: Test Suite
        caches:
          - node
        script:
          - npm ci
          - npm run test
    - step:
        name: Build Frontend
        caches:
          - node
        script:
          - npm ci
          - npm run build
